name: Testes Automatizados

on:
  push:
    branches:
      - main

env:
  PYTHONPATH: Python

jobs:
  test-soma:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: pip install pytest

      - name: Testar soma
        run: pytest Python/tests/test_soma.py

  test-subtracao:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: pip install pytest

      - name: Testar subtração
        run: pytest Python/tests/test_subtracao.py

  test-multiplicacao:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: pip install pytest

      - name: Testar multiplicação
        run: pytest Python/tests/test_multiplicacao.py

  test-divisao:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: pip install pytest

      - name: Testar divisão
        run: pytest Python/tests/test_divisao.py